# Service interfaces

DI protocols for admin, daemon, notification, and related services.  
`ISettingsService` is defined in `plugin_system.interfaces` and re-exported here.

::: GUI.app.services.interfaces
